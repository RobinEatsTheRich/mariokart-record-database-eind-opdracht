
# datasource PostgreSQl
spring.sql.init.platform=postgres
#Ik heb hem weer terug gezet naar Mariokart in plaats van Test
spring.datasource.url=jdbc:postgresql://localhost:5432/Mariokart
spring.datasource.username=postgres
#This is a random password, don't bother
spring.datasource.password=#4I&Lx$j7THC
spring.datasource.driver-class-name=org.postgresql.Driver

# jpa
spring.jpa.database=postgresql
spring.jpa.show-sql=true

# generate schema ddl to create tables
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=create


# database initialization with data.sql after hibernate
# DEPRECATED spring.datasource.initialization-mode=always
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
